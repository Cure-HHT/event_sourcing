# Central version pinning for hht_admin's CI workflows and for consumer
# repos that copy these workflows from this repo. Source of truth for
# the org-shared subset of versions.
#
# Consumer repos (event_sourcing, etc.) may extend this file with
# additional pins specific to their toolchain.

# Gitleaks (secret scanning) — used by secrets-scan.yml
GITLEAKS_VERSION=8.29.0
