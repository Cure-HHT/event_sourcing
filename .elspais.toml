# elspais configuration — Cure-HHT Event Sourcing Library
#
# Defines the formal-requirement ID format, level hierarchy, and file
# scanning surface for this repo. Authored by hand (not generated by
# `elspais init`) to use the EVS namespace with named (not numeric)
# component IDs.
#
# Companion: spec/requirements-spec.md (canonical grammar).

# Schema version (do not change without consulting elspais docs)
version = 4

# Daemon TTL in minutes (>0 = auto-start, 0 = disabled, <0 = no timeout)
cli_ttl = 30
# File path for MCP tool-usage statistics (optional)
stats = ""

# Project identity

[project]
namespace = "EVS"
name = "Cure-HHT Event Sourcing Library"

# Requirement ID format
#
# Canonical form: EVS-{level}-{component}[-{assertion}]
#   level     — three-letter audience tag (`prd` or `dev`)
#   component — kebab-case noun describing the requirement subject
#   assertion — optional A–Z label (or A+B+C... multi-assertion)
#
# Examples:
#   EVS-prd-event-store
#   EVS-dev-provenance-entry-schema-G
#   EVS-dev-canonical-json

[id-patterns]
canonical = "{namespace}-{level.letter}-{component}"
separators = ["-"]
prefix_optional = false

[id-patterns.aliases]
short = "{level.letter}-{component}"

[id-patterns.component]
# `named` = kebab-case noun. `numeric` = NNNNN form (used by hht_diary
# but rejected here in favor of human-meaningful component names).
style = "named"
max_length = 64
# pattern = ""  # alphanumeric style only
# digits = 5    # numeric style only
# leading_zeros = true

[id-patterns.assertions]
label_style = "uppercase"
max_count = 26
multi_separator = "+"
zero_pad = false

[id-patterns.associated]
enabled = false
position = "after_prefix"
format = "uppercase"
length = 3
separator = "-"

# Requirement-level definitions
#
# This repo uses two levels: prd and dev.
# Ops level is intentionally not used — the library is pure-Dart with
# no deployment / runtime-ops surface; ops obligations live in
# consuming app repos (e.g. cure-hht/hht_diary).

[levels.prd]
rank = 1
letter = "prd"
display_name = "Product"
implements = ["prd"]

[levels.dev]
rank = 2
letter = "dev"
display_name = "Development"
implements = ["dev", "prd"]

# File scanning configuration

[scanning]
skip = ["node_modules", ".git", "__pycache__", "*.pyc", ".venv", ".env", ".dart_tool", "build", ".flutter-plugins", ".flutter-plugins-dependencies"]

[scanning.spec]
directories = ["spec"]
file_patterns = ["*.md"]
# Authoring infrastructure files, not normative requirements.
skip_files = ["README.md", "requirements-spec.md", "INDEX.md", "EXAMPLE-requirement.md"]

[scanning.code]
directories = ["event_sourcing/lib", "canonical_json_jcs/lib", "provenance/lib"]
file_patterns = []
skip_files = []
skip_dirs = []

[scanning.test]
directories = [
  "event_sourcing/test",
  "event_sourcing/integration_test",
  "event_sourcing/example_action_permissions/test",
  "event_sourcing/example/test",
  "canonical_json_jcs/test",
  "provenance/test",
]
file_patterns = ["*_test.dart", "test_*.dart"]
skip_files = []
skip_dirs = []
enabled = true
prescan_command = ""
reference_keyword = "Verifies"
reference_patterns = []

[scanning.result]
directories = ["build-reports"]
file_patterns = ["**/TEST-*.xml", "pytest-results.json"]
skip_files = []
skip_dirs = []
run_meta_file = ""

[scanning.coverage]
directories = ["."]
file_patterns = []
skip_files = []
skip_dirs = []

[scanning.docs]
directories = ["docs"]
file_patterns = ["*.md"]
