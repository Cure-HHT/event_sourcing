# Analyzer options of the third-party backend fixture the internal-member
# test analyzes out of package: a use of an internal library member is an
# error.
analyzer:
  errors:
    invalid_use_of_internal_member: error
